{
  "name": "ip-public",
  "module": "app.ts",
  "type": "module",
  "scripts": {
    "dev": "bun app.ts",
    "build": "bun build --entrypoints ./app.ts --outdir ./ --target bun",
    "start": "bun app.js"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0",
    "@types/node-cron": "^3.0.11"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "node-cron": "^3.0.3"
  }
}